import { useDispatch, useSelector } from 'react-redux';

/** Аліаси на випадок переходу на TypeScript — щоб імпорти не мінялись. */
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;
